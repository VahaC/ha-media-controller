#ifndef T560_TEST_PANEL_CARDS_H
#define T560_TEST_PANEL_CARDS_H

/* Registered from the single test binary's main, so that the rules a card's
 * display name and icon have to pass are covered by the same `make test`
 * run. */
void panel_cards_tests_register(void);

#endif
