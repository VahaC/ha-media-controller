#ifndef T560_TEST_PANEL_GRID_H
#define T560_TEST_PANEL_GRID_H

/* Registered from the single test binary's main, so that the layout the room
 * page is built from is covered by the same `make test` run. */
void panel_grid_tests_register(void);

#endif
