# Legacy Home Assistant implementation

The files in this directory are the original working Pyscript services and
manual automations. They remain available during migration and are also copied
unchanged under `legacy/home-assistant/`.

New installations should use `custom_components/vahac_media_controller/`.
Existing installations must keep the legacy setup until the verification steps
in the root README pass on their own system.

